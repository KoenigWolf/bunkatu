// =============================
// PostCSS 設定
// =============================

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS を適用
    tailwindcss: {},
  },
};

export default config;
